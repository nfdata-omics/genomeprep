/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
----------------------------------------------------------------------------------------
*/

process {

    publishDir = [
        path: { "${params.outdir}/${task.process.tokenize(':')[-1].tokenize('_')[0].toLowerCase()}" },
        mode: params.publish_dir_mode,
        saveAs: { filename -> filename.equals('versions.yml') ? null : filename }
    ]

    withName: 'CELLRANGER_MKGTF' {
        ext.args = "--attribute=gene_biotype:protein_coding \
                --attribute=gene_biotype:protein_coding_LoF \
                --attribute=gene_biotype:lincRNA \
                --attribute=gene_biotype:lncRNA \
                --attribute=gene_biotype:antisense \
                --attribute=gene_biotype:IG_LV_gene \
                --attribute=gene_biotype:IG_V_gene \
                --attribute=gene_biotype:IG_V_pseudogene \
                --attribute=gene_biotype:IG_D_gene \
                --attribute=gene_biotype:IG_J_gene \
                --attribute=gene_biotype:IG_J_pseudogene \
                --attribute=gene_biotype:IG_C_gene \
                --attribute=gene_biotype:IG_C_pseudogene \
                --attribute=gene_biotype:TR_V_gene \
                --attribute=gene_biotype:TR_V_pseudogene \
                --attribute=gene_biotype:TR_D_gene \
                --attribute=gene_biotype:TR_J_gene \
                --attribute=gene_biotype:TR_J_pseudogene \
                --attribute=gene_biotype:TR_C_gene \
                --attribute=transcript_biotype:protein_coding \
                --attribute=transcript_biotype:protein_coding_LoF \
                --attribute=transcript_biotype:lincRNA \
                --attribute=transcript_biotype:lncRNA \
                --attribute=transcript_biotype:antisense \
                --attribute=transcript_biotype:IG_LV_gene \
                --attribute=transcript_biotype:IG_V_gene \
                --attribute=transcript_biotype:IG_V_pseudogene \
                --attribute=transcript_biotype:IG_D_gene \
                --attribute=transcript_biotype:IG_J_gene \
                --attribute=transcript_biotype:IG_J_pseudogene \
                --attribute=transcript_biotype:IG_C_gene \
                --attribute=transcript_biotype:IG_C_pseudogene \
                --attribute=transcript_biotype:TR_V_gene \
                --attribute=transcript_biotype:TR_V_pseudogene \
                --attribute=transcript_biotype:TR_D_gene \
                --attribute=transcript_biotype:TR_J_gene \
                --attribute=transcript_biotype:TR_J_pseudogene \
                --attribute=transcript_biotype:TR_C_gene"
    }

    withName: 'CELLRANGER_MKREF' {
        time = '48.h'
        publishDir = [
            path: { "${params.outdir}/cellranger" },
            mode: params.publish_dir_mode,
            saveAs: { f -> f == 'versions.yml' ? null : f.replace("$reference_name", "${reference_name}_cellranger") }
        ]
    }

    withName: 'CELLRANGER_MKVDJREF' {
        time = '48.h'
        publishDir = [
            path: { "${params.outdir}/cellranger_vdj" },
            mode: params.publish_dir_mode,
            saveAs: { f -> f == 'versions.yml' ? null : f.replace("$reference_name", "${reference_name}_cellranger_vdj") }
        ]
    }

    withName: 'CELLRANGERATAC_MKREF' {
        time = '48.h'
        publishDir = [
            path: { "${params.outdir}/cellranger_atac" },
            mode: params.publish_dir_mode,
            saveAs: { f -> f == 'versions.yml' ? null : f.replace("$reference_name", "${reference_name}_cellranger_atac") }
        ]
    }

    withName: 'SPACERANGER_MKREF' {
        time = '48.h'
        publishDir = [
            path: { "${params.outdir}/spaceranger" },
            mode: params.publish_dir_mode,
            saveAs: { f -> f == 'versions.yml' ? null : f.replace("$reference_name", "${reference_name}_spaceranger") }
        ]
    }
}
