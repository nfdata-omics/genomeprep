Changes in component 'nf-core/spaceranger/mkref'
'modules/nf-core/spaceranger/mkref/meta.yml' is unchanged
Changes in 'spaceranger/mkref/main.nf':
--- modules/nf-core/spaceranger/mkref/main.nf
+++ modules/nf-core/spaceranger/mkref/main.nf
@@ -10,7 +10,7 @@
     val reference_name
 
     output:
-    path "${reference_name}", emit: reference
+    path "${reference_name}_spaceranger", emit: reference
     path "versions.yml"     , emit: versions
 
     when:
@@ -26,6 +26,8 @@
     // --nthreads is passed to the STAR index generation.
     // see also https://github.com/nf-core/scrnaseq/issues/329
     """
+    mkdir -p "${reference_name}_spaceranger"
+
     spaceranger \\
         mkref \\
         --genome=$reference_name \\
@@ -35,6 +37,8 @@
         --localmem=${task.memory.toGiga()} \\
         --nthreads=${task.cpus} \\
         $args
+
+    mv "${reference_name}" "${reference_name}_spaceranger"
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":

'modules/nf-core/spaceranger/mkref/tests/main.nf.test' is unchanged
'modules/nf-core/spaceranger/mkref/tests/main.nf.test.snap' was removed
************************************************************
